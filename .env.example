# Ejemplo de variables de entorno para PageSpeed Analyzer
# Copia este archivo como .env y ajusta los valores según tu entorno

# Configuración de la aplicación
NODE_ENV=production
PORT=5000
HOSTNAME=0.0.0.0

# Configuración de la base de datos PostgreSQL
DATABASE_URL=postgresql://pagespeed_user:pagespeed_pass@postgres:5432/pagespeed_db
PGHOST=postgres
PGPORT=5432
PGUSER=pagespeed_user
PGPASSWORD=pagespeed_pass
PGDATABASE=pagespeed_db

# Para despliegue local, puedes cambiar el host de la base de datos
# DATABASE_URL=postgresql://pagespeed_user:pagespeed_pass@localhost:5432/pagespeed_db
# PGHOST=localhost